/*
* librerias más importantes:
#include <stdio.h>
#include <stdlib.h>

* agregar variables
tipo variable (= valor);
int / float / double / char / long long 

* info relevante para funciones
int argc cantidad de inputs del usuario
char* argv[] lista de strings para almacenar los inputs

int main(int argc, char* argv[]){
    printf("%i", variable); %i %f etc... para incluir variables dentro de un string
    argc = cantidad de inputs
    argv[1] tomaria el primer input del usuario
    atoi() -> transforma input a int
    return 0; siempre retornar el tipo de la función (void para no tener que retornar nada)
}
*/